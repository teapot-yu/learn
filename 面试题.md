## 搜狗
1. react fiber解决什么问题
2. 浏览器事件循环与node事件循环的区别
3. 微任务执行微任务怎么排队
4. koa和express的区别，洋葱模型知道吗？
5. react hooks为了解决什么问题？
6. flex布局有用过吗？
7. echarts富文本有用过吗？
8. antd源码读过吗？有没有进行过封装。
9. instanceof如何实现
10. indexOf如何实现
11. 为什么离职

## 好未来
1. 说一下promise实现过程
2. js脚本中async和defer实现

## 微医
1. 实现继承的几种方式
2. vue中的双向绑定如何实现的
3. 浏览器的时间循环和node的事件循环有什么不同？
4. 介绍下css盒模型
5. flex 1代表啥
6. 什么是单点登录

## 作业帮
1. 性能监控监控了哪些指标


## 快手
1. npm包如何实现按需加载
2. setState如何更新dom

react合成事件和钩子函数本身就处于一个大的事物中，会先将isBatchingUpdate设置为true，在执行到setState是判断该变量为true，会将当前组件放到dirtyComponents中，在当前事物被close的时候，会再去遍历dirtyComponent,执行相应的更新操作。
而在自定义事件和setTimeout里，会直接执行setState，此时isBatchingUpdate为false，里面则会执行同步更新。

3. npm包如何兼容不同的版本和js代码格式
4. react里面使用过哪些新特性
5. 排序数组实现lastIndex方法
6. 资源报错如何监控
7. 资源性能如何监控
8. 解析URl
9. new Vue发生了啥
10. 实现hex方法，生成随机颜色"#ffcc00"
11. 实现backHex方法，将颜色转成对应rgb即#ffcc00 => rgb(255, 255, 0)
12. css如果中间div设置为position: absolute,位置变化
```
<body>
  <a>a<a>
  <div></div>
  <input>
</body>
```
13. class = 'a b'哪个会生效
14. 生成区间内的随机数

## 爱奇艺&一点资讯
1. 什么是闭包
2. 防止内存泄漏
3. 圣杯布局和双飞翼布局的实现
https://www.cnblogs.com/jiguiyan/p/11425276.html
4. js的垃圾回收机制
5. 实现防抖和节流
6. 实现一个二叉树的广度优先遍历
7. v-model的实现原理
8. vue和react的差别，为什么从vue过渡到了react
9. vue diff算法的过程
10. 实现一个正方形
11. 实现一个三角形
12. canvas有了解过吗
13. 小程序的渲染过程有了解过吗
14. webpack原理
15. 从输入一个url都经历了哪些
16. cdn原理
17. 说一下tcp三次握手
18. 说一下你对原型链的理解
19. animation和transition的区别
20. nocache和nostore的区别
21. BFC
22. position 的几种类型
23. http的contenttype有哪些类型
24. js中的垃圾回收机制
25. flex: 1
26. 遇到过哪些兼容性问题
27. 小程序遇见过哪些坑
28. http简单请求，
29. cdn不会发送option请求
30. option请求
31. 洗牌问题
```
const p = new Promise((resolve) => {
  console.log(1)
  resolve(5)
  console.log(2)
})
p.then((res) => {
  console.log(res)
})
```

## 头条

1. HTTP OPTION 请求的作用，发送内容
https://blog.csdn.net/luanxiyuan/article/details/100137496
2. http同源策略
3. 简单请求是什么？CDN 资源：不算，因为是简单请求
4. 实现一个jsonp
5. 实现一个柯里化函数
6. css-loader 与 style-loader  作用
7. 实现一个fetchAll
8. react中Key的作用
9. 高阶组件有什么问题
10. Hooks解决了什么问题
11. 在开发mock平台中调研了哪些组件
12. ssr的实现过程
13. 实现一个flatten函数
14. 三数之和
15. 为什么从vue过渡到react
16. v-model适用场景有哪些
17. 如何推动react技术的推广，是不是做了些demo？
18. 是不是与增加测试用例如何配置测试用例
19. webpack打包做了哪些操作

## 腾讯
1. 使用过哪些格式的图片，他们的区别是什么， webp是什么格式？
https://segmentfault.com/a/1190000013589397

2. 以下代码会输出什么？
```
let a = { n: 1 }
let b = a
a.x = a = { n: 2 }
console.log(a)
console.log(a.x)
console.log(b)
console.log(b.x)
```
3. base64后图片的体积变大了还是变小了？如果变大了那位什么还要用？
4. webpack按需加载原理
5. 小程序使用中遇到过什么问题吗？
6. 遇到过什么兼容性。
7. CDN原理
请求本地dns缓存，如果有则直接使用对应IP，如果没有，则请求到dns解析器，同时通过该dns解析器的CNAME指向，只能DNS解析系统，得到性能最好的cdn服务器对应的ip。


## 阿里健康

1. redux-thunk和redux-saga的区别
2. 事件循环


## 滴滴

1. js 实现二分查找算法，并写出时间复杂度。
2. js 实现深拷贝
3. 如下代码弹出什么信息
4. 用flex实现如下效果，容器宽高不定，子元素宽高固定
5. 现有瀑布流式图片页面，用js监听每个图片点击事件
6. 执行如下代码，显示什么，点击每个a标签弹出什么？
7. 执行如下代码输出什么
8. 执行以下代码输出什么
9. 执行如下代码，abc是什么颜色
10. 查询输入框，输入调用接口进行查询，返回结果后进行渲染。现进行优化，降低查询频率，200ms内不多于1次查询


```
document.body.addEventListen('click', (e) => {
  if(e.target.elementType === 'img') {
    doSome(e)
  }
})
```

## 美团

1. 实现节流函数
2. bfs实现
3. 判断字符串中是否包含数字
4. webpack loader和plugin的区别
5. for in、for of、forEach的区别
6. 小程序有哪些坑
7. TypeScript用过吗？
8. 以下代码的执行顺序
```
console.log('1111')
new Promise((resovle) => {
  console.log('22222')
  resolve()
  console.log('33333')
})
console.log('4444)
```
9. 写过webpack插件吗？
10. 字符串去掉注释
11. webpack 打包速度优化
13. 什么是dll
14. 前端组件如何实现懒加载
15. 有没有针对react的优化
16. 在真实的项目开发中有没有做过优化
17. 实现一个promise.all
18. 你自己遇到有挑战的项目
19. 在性能优化方面做过哪些工作
20. AST语法树有没有了解过
21. webpack引入uplfy压缩有什么问题
22. antd源码有了解过吗？
23. 如何快速渲染10000条数据
24. requestAnimationFrame，requestIdleCallback的区别和应用
```

console.log(1);
setTimeout(() => {
  console.log(2);
  process.nextTick(() => {
    console.log(3);
  });
  new Promise((resolve) => {
    console.log(4);
    resolve();
  }).then(() => {
    console.log(5);
  });
});
new Promise((resolve) => {
  console.log(7);
  resolve();
}).then(() => {
  console.log(8);
});
process.nextTick(() => {
  console.log(6);
});
console.log(20)
setTimeout(() => {
  console.log(9);
  process.nextTick(() => {
    console.log(10);
  });
  new Promise((resolve) => {
    console.log(11);
    resolve();
  }).then(() => {
    console.log(12);
  });
});

setTimeout(()=>{
  console.log('timer1');
  Promise.resolve().then(function() {
      console.log('promise1');
  });
});

setTimeout(()=>{
  console.log('timer2');
  Promise.resolve().then(function() {
      console.log('promise2');
  });
});
```

```
立即调用的函数表达式（IIFE） 有一个 自己独立的 作用域，如果函数名称与内部变量名称冲突，就会永远执行函数本身；所以上面的结果输出是函数本身；
var a = 1;
(function a () {
    a = 2;
    console.log(a);
})();
```

## 快手

```
<style>
div {
color: red;
}
#title {
color: yellow;
}
div.title {
color: blue;
}
</style>
<div class="title" id="title">abc</div>
```
```
<style>
.classA {
color: blue;
}
.classB {
color: red;
}
</style>
<p class="classB classA">123</p>
```
3. flex
三列布局，两边固定宽度，中间自适应
4
```
console.log('script start')

async function async1() {
await async2()
console.log('async1 end')
}

async function async2() {
console.log('async2 end')
}
async1()

setTimeout(function() {
console.log('setTimeout')
}, 0)

new Promise(resolve => {
console.log('Promise')
resolve()
})
.then(function() {
console.log('promise1')
})
.then(function() {
console.log('promise2')
})

console.log('script end')

```

5. faltten
6. 排序数组合并
7. 9小球，空心的1个 。天平找出
8. react hooks实现settimeout
